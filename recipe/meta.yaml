{% set name = "apeye-core" %}
{% set version = "1.1.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/apeye_core-{{ version }}.tar.gz
  sha256: 06f9c147b12b24ceafefe6a48f52ac628e3b94d2df375f1b88c0aa41d6b24276

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - hatch-requirements-txt
    - pip
    - python >=3.6
  run:
    - domdf-python-tools >=2.6.0
    - idna >=2.5
    - python >=3.6

test:
  imports:
    - apeye_core
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/apeye-core/
  summary: Core (offline) functionality for the apeye library.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - domdfcoding
